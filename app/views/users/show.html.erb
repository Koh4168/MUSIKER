<div class="contents row">
    
    
   <div class="user_events">
        <% @events.each do |event| %>
        <p>
       <%=image_tag(@event.image_url) %>
        <%= event.name%> 
        <%= event.price%>
        <%= event.scedule%>
        </p>
        </div>
        <% end %>
        <%= paginate(@events) %>
    </div>
   
  
 
</div>