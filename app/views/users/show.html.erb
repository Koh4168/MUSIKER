<div class="contents row">
    <p><%= @name　%>さん</p>
    <% @events.each do |event| %>
    
    <p><%= event.name%></p>
      <%= simple_format(event.text) %>
     <p><%= event.image%></p>
     <p><%= event.place%></p>
     <p><%= event.price%></p>
     <p><%= event.scedule%></p>
    </div>
  <% end %>
  <%= paginate(@events) %>
 
</div>