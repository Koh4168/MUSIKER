<div class="events">
         <%=link_to (image_tag event.image.url),"/events/#{event.id}"%>
         <div class="event_name">
                  <%= event.name%>
         </div>
         <div class="event_place">
                  <%= event.place%>
         </div>
         <div class="event_scedule">
                  <%= event.scedule%>
         </div>
  
     
     
     <div class="user_name">
         <%= link_to "投稿者#{event.user.name}", user_path(event.user) %><br>
         <%= link_to '詳細',"/events/#{event.id}" %>
    </div>
   </div>




